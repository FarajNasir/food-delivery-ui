"use client";

// Location prompt has been moved to the cart page (CustomerCart.tsx).
// This component is kept as a no-op so the import in layout.tsx stays valid.
export default function LocationManager() {
  return null;
}
